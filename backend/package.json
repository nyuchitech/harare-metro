{
  "name": "harare-metro-backend",
  "version": "1.0.0",
  "description": "Backend API and Admin Interface for Harare Metro",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run clean && npm run typecheck && npm run build && wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Backend: TypeScript compilation serves as linting'",
    "validate": "npm run typecheck && npm run build",
    "clean": "rm -rf node_modules/.cache .wrangler dist build && npm cache clean --force",
    "install:clean": "npm run clean && npm install",
    "db:migrate": "wrangler d1 execute hararemetro_articles --remote --file ./worker/database/schema.sql",
    "db:local": "wrangler d1 execute hararemetro_articles --local --file ./worker/database/schema.sql",
    "db:schema": "echo 'Database schema located at: ./worker/database/schema.sql'"
  },
  "dependencies": {
    "hono": "^4.9.7",
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250425.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.21.2"
  }
}